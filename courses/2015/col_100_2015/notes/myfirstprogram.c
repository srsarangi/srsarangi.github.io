#include <stdio.h>

int main() {

	/* I am printing three times */
	/* printf ( " <format string> ", .. , .. , ..); */
	/* We have learnt : \n , %d, printf */
	/* %d --> base 10 integer , %f --> floating point number */
	/* %e --> scientific notation , %g --> chooses between %e and %f*/
	/* %x --> hexadecimal (base 16), %o --> octal (base 8) */
	/* %# --> enhances readability of %x and %o */
	/* %c --> character (single letter). It is necessary to write a character between
	   ' and ' */
	/* %s --> string */
	/* %.2f --> 2 digits after the decimal point */
	/* %6d --> Print 6 digits (if digits are not there, replace by spaces */
	/* You want to print %, Write %% in the format string */
	/* You want to print \, write \\ in the format string */ 
	   

	printf(" ----- |||||");


}
