#include <stdio.h>

int main() {
	/* Don't look above this line */
	/* basic operators: + - * / (divide)  */ 
	float num; 
	int rounded_num;
	
	scanf("%f", &num);
	rounded_num = num + 0.5;

	printf("%d \n", rounded_num);

	/* Don't look below this line */
}
